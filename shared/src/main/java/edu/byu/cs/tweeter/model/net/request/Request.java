package edu.byu.cs.tweeter.model.net.request;

public class Request {
    /**
     * Allows construction of the object from Json. Private (outside of package) so it won't be called in normal code.
     */
    Request() {}
}
